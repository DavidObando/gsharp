// inventory: RefExpression — QUARANTINED at translate: CS2GS-GAP "expression
// 'RefExpression' has no canonical G# form yet; emitted an identifier
// placeholder (ADR-0115 §B)." for all forms: 'ref int r = ref xs[1]',
// 'return ref a[i]', 'ref int q = ref Pick(xs, 2)', 'ref int alias = ref v'.
using System;

namespace Corpus.Grid09
{
    public static class RefExpressionFixture
    {
        public static void Run()
        {
            int[] xs = { 1, 2, 3 };

            // Ref local aliasing an array element.
            ref int r = ref xs[1];
            r = 20;

            // Ref-returning local function.
            static ref int Pick(int[] a, int i)
            {
                return ref a[i];
            }

            ref int q = ref Pick(xs, 2);
            q = 30;
            Console.WriteLine($"RefExpression: xs={string.Join(",", xs)}");

            int v = 5;
            ref int alias = ref v;
            alias = 6;
            Console.WriteLine($"RefExpression: v={v}");
        }
    }
}
